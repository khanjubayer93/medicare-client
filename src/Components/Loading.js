import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center h-[700px]'><p className='font-bold'>Loading...</p></div>
    );
};

export default Loading;